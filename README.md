# X-UI Telegram Bot

Production Telegram-бот для продажи подписок и администрирования панели 3x-ui (MHSanaei/3x-ui) через Telegram.

## Возможности

### 🛍 Продажи и подписки
- Оплата через Telegram Stars и автоматическая выдача доступа после успешного платежа
- Продление действующей подписки и переактивация при истечении
- Пробный период на 3 дня (с защитой от повторного использования)
- Учёт транзакций и базовая идемпотентность обработки оплат
- Автозаполнение комментариев в 3x-ui никнеймом Telegram для удобства администрирования

### 👤 Личный кабинет
- 🚀 Мой конфиг: ссылка подписки, ключ доступа, QR‑код, инструкции по подключению
- 📊 Моя статистика: трафик за день/неделю/месяц/всего и срок действия подписки
- Корректная обработка бессрочной подписки: «Бессрочный»

### 👮‍♂️ Админ‑панель
- 📊 Сводная статистика: пользователи, онлайн, доход, продажи
- Управление клиентами 3x-ui:
  - списки: все / активные / истекающие / онлайн / пробные / истекшие
  - карточка клиента: трафик, статус, TG ID, никнейм, срок, лимит устройств
  - изменение лимита устройств (0 = бессрочно/без ограничения)
  - история IP подключений с флагами стран (GeoIP)
  - синхронизация никнеймов (обновление comment в 3x-ui)
  - rebind (привязка клиента к Telegram ID) и служебные действия (сброс trial, удаление)
- 🏆 Leaderboard:
  - рейтинг по трафику за месяц
  - рейтинг по оставшимся дням подписки

### 🛡 Безопасность и мониторинг
- ⚠️ Мульти‑IP детектор: обнаружение одновременных подключений одним конфигом
- GeoIP: определение страны по IP (для логов/истории)
- Мониторинг сервера: CPU, RAM, Disk и live‑трафик

### 📣 Маркетинг и коммуникации
- Рассылки: всем пользователям или по сегментам (RU/EN)
- Опросы: создание и обновляемая статистика голосования
- Промокоды: выдача дней, лимиты использования, история применений
- Flash‑промо: временные сообщения с автоудалением
- Поддержка: форвардинг сообщений пользователем администратору (опционально через support‑бота)

### 💾 Бэкапы и восстановление
- Создание бэкапов BOT DB и X-UI DB
- Восстановление из набора бэкапов и из загруженного файла
- Автоочистка по количеству файлов/наборов (настраивается через env)

### 🧩 Версии и обновления
- Отображение версий 3x-ui и Xray
- Кнопка обновления 3x-ui / Xray из бота (с проверкой версий)
- Перезапуск сервисов 3x-ui и бота из админ‑панели

## Архитектура и данные

- Бот: [bot/bot.py](bot/bot.py)
- Сервис запуска: systemd (по умолчанию `x-ui-bot`, либо `x-ui-bot-<slug>` при multi‑bot установке)
- Базы данных:
  - X-UI DB (по умолчанию): `/etc/x-ui/x-ui.db` (`XUI_DB_PATH`)
  - BOT DB (по умолчанию): `/usr/local/x-ui/bot/bot_data.db` (`BOT_DB_PATH`)

## Установка (production)

### Вариант 1 — через installer (рекомендуется)

На Ubuntu 22.04 под root:

Быстрый старт:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/prokazzzzza/x-ui-Telegram_bot/main/install.sh)
```

Локально из репозитория:

```bash
cd /usr/local/x-ui
sudo bash bot/install_bot.sh
```

Скрипт:
- проверяет/устанавливает 3x-ui (MHSanaei/3x-ui) и обновляет 3x-ui/Xray
- разворачивает virtualenv и зависимости
- создаёт systemd‑сервис и отдельный `.env` для выбранного имени бота
- проверяет ввод HOST_IP и создаёт каталог логов

### Вариант 2 — ручная установка

1) Python 3.10+ и 3x-ui должны быть установлены заранее
2) Создайте виртуальное окружение и установите зависимости:

```bash
cd /usr/local/x-ui/bot
python3 -m venv venv
./venv/bin/pip install -U pip
./venv/bin/pip install -r requirements.txt
```

3) Создайте `.env` (или `.env.<slug>` для multi‑bot) и укажите переменные окружения (см. ниже)
4) Создайте systemd‑service по аналогии с installer и запустите

## Переменные окружения (.env)

Обязательные:
- `BOT_TOKEN` — токен основного бота
- `ADMIN_ID` — Telegram ID администратора
- `HOST_IP` — публичный IP сервера для формирования ссылок/конфига

Рекомендуемые:
- `BOT_NAME` — отображаемое имя (используется в installer и логике multi‑bot)
- `SUPPORT_BOT_TOKEN` — токен support‑бота (если включаете поддержку через отдельного бота)
- `XUI_DB_PATH` — путь к базе 3x-ui (по умолчанию `/etc/x-ui/x-ui.db`)
- `BOT_DB_PATH` — путь к базе бота (по умолчанию `/usr/local/x-ui/bot/bot_data.db`)
- `BOT_LOG_DIR` — каталог логов (по умолчанию `/usr/local/x-ui/logs`)
- `BOT_LOG_FILE` — файл логов (по умолчанию `<BOT_LOG_DIR>/bot.log`)
- `XUI_SYSTEMD_SERVICE` — имя systemd‑сервиса панели (по умолчанию `x-ui`)
- `BOT_SYSTEMD_SERVICE` — имя systemd‑сервиса бота (по умолчанию `x-ui-bot`)

Дополнительно:
- `HOST_PORT` — порт (если нужен для генерации ссылок)
- `REF_BONUS_DAYS` — бонус за реферала (дней, по умолчанию 7)
- `BACKUP_KEEP_FILES` — сколько файлов бэкапов хранить (по умолчанию 20)
- `BACKUP_KEEP_SETS` — сколько наборов бэкапов хранить (по умолчанию 20)
- `SUSPICIOUS_EVENTS_LOOKBACK_SEC` — глубина истории для мульти‑IP (по умолчанию 86400)
- `SALES_LOG_DEDUPE_WINDOW_SEC` — окно дедупликации логов продаж (по умолчанию 600)
- `SALES_LOG_FUZZY_CHARGE_DEDUPE_WINDOW_SEC` — окно «похожих» оплат (по умолчанию 60)

## Управление сервисами

Проверить статус/логи:

```bash
systemctl status x-ui
systemctl status x-ui-bot
journalctl -u x-ui-bot -n 200 --no-pager
```

Если установлен multi‑bot, сервис будет иметь имя вида `x-ui-bot-<slug>`.

## Разработка и тесты

Команды из корня репозитория:

```bash
./bot/venv/bin/python -m ruff check --ignore=E701,E722 bot tests
./bot/venv/bin/python -m mypy --ignore-missing-imports bot tests
./bot/venv/bin/python -m pytest -q
```

Pre-commit:
- конфиг: [.pre-commit-config.yaml](.pre-commit-config.yaml)
- рекомендуется установить и включить хуки: `pre-commit install`
